## Creating Testing Certs

First, follow the installation guide from the [cfssl](https://github.com/cloudflare/cfssl) README.

Then, generate the certs:

```
cd agent/jvm/src/test/resources/certs

./build.sh
```
